import { supabase } from "../lib/supabase";

export const api = {
  auth: {
    supabase,
  },
  products: {
    supabase,
  },
  cart: {
    supabase,
  },
  wishlist: {
    supabase,
  },
  orders: {
    supabase,
  },
};
